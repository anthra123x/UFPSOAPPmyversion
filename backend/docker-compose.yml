version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ufpso_backend_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
